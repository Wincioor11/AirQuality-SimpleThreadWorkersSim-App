Simple app which simulates threads as workers with access to global data structure, created while teaching friend how to 
code in python and synchronise threads.

libraries in use:
- threading
- requests
